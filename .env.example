# ========================================
# CONFIGURATION GÉNÉRALE
# ========================================

# Environnement (development ou production)
NODE_ENV=development

# Port du serveur Express
PORT=3001

# Adresse d'écoute du serveur (0.0.0.0 pour toutes les interfaces)
HOST=0.0.0.0

# URL de l'API backend (utilisée par le frontend via Vite)
# En développement : http://localhost:3001/api
# En production : /api (URL relative, le serveur Express sert tout)
VITE_API_URL=/api

# URL de base pour les liens de saisie CRA
# En développement : http://localhost:5173
# En production : https://votre-domaine.com
BASE_URL=http://localhost:5173

# ========================================
# CONFIGURATION SMTP (Email)
# ========================================

# IMPORTANT: Si votre mot de passe contient des caractères spéciaux (#, $, &, etc.),
# mettez-le entre guillemets doubles. Exemples:
# SMTP_PASS="mon-mot-de-passe#123"
# SMTP_PASS="password$special&chars"

# Hôte SMTP
SMTP_HOST=smtp.ethereal.email

# Port SMTP (587 pour TLS, 465 pour SSL)
SMTP_PORT=587

# Nom d'utilisateur SMTP
SMTP_USER=your-email@example.com

# Mot de passe SMTP (mettez-le entre guillemets s'il contient des caractères spéciaux)
SMTP_PASS="your-password"

# ========================================
# CONFIGURATION FACTURATION
# ========================================

# Taux de TVA en pourcentage
TAUX_TVA=20

# Préfixe des numéros de facture (ex: FAC-2024-001)
INVOICE_PREFIX=FAC

# Nombre de chiffres pour le numéro de facture (padding)
INVOICE_NUMBER_PADDING=3

# Délai de facturation par défaut (en jours)
DEFAULT_DELAI_FACTURATION=30

# Nombre max de jours travaillés par mois
MAX_JOURS_TRAVAILLES=31

# ========================================
# CONFIGURATION PÉNALITÉS DE RETARD
# ========================================

# Multiplicateur du taux d'intérêt légal pour pénalités de retard
PENALITE_TAUX_INTERET_LEGAL=3

# Indemnité forfaitaire de recouvrement (en euros)
INDEMNITE_RECOUVREMENT=40

# ========================================
# CONFIGURATION TOKENS CRA
# ========================================

# Durée de validité d'un token CRA (en jours)
CRA_TOKEN_EXPIRATION_DAYS=10

# Durée de rétention des tokens expirés avant nettoyage (en jours)
CRA_TOKEN_CLEANUP_DAYS=30

# ========================================
# PLANIFICATION CRON (Rappels CRA)
# ========================================

# Expression cron pour l'envoi des rappels CRA
# Format: minute heure jour mois jour_semaine
# Par défaut: '0 9 25 * *' = 9h00 le 25 de chaque mois
CRA_CRON_SCHEDULE=0 9 25 * *

# Fuseau horaire pour la planification
CRA_CRON_TIMEZONE=Europe/Paris

# Délai entre chaque envoi d'email (en millisecondes)
EMAIL_SEND_DELAY_MS=1000

# ========================================
# CONFIGURATION SMS (Twilio)
# ========================================

# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ========================================
# CONFIGURATION POUR DIFFÉRENTS PROVIDERS SMTP
# ========================================

# Pour Gmail (nécessite d'activer "Autoriser les applications moins sécurisées")
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre-email@gmail.com
# SMTP_PASS=votre-mot-de-passe

# Pour SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=votre-clé-api-sendgrid

# Pour Office365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=votre-email@outlook.com
# SMTP_PASS=votre-mot-de-passe

# Pour obtenir un compte de test Ethereal gratuit:
# 1. Démarrez le serveur avec: npm run server
# 2. Le serveur créera automatiquement un compte de test si aucune configuration n'est fournie
# 3. Copiez les informations affichées dans la console dans ce fichier